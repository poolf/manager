/* ManagerHeader controller */
import Manaheader from './managerHeader.vue'
const ManagerHeader = {
  install: function (Vue) {
    Vue.component('m-header', Manaheader)
  }
}

export default ManagerHeader
